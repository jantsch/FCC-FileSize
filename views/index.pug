extends layout

block content
  h3  Submit a file to view its filesize. 
  form(method='POST' action='/get-file-size' enctype="multipart/form-data")
    div.form-group
      label.btn.btn-primary Browse 
          input(type='file',name='file',id='fileid') 
    <br>

    label(id='name')  No File Selected ! 
    
    <br>
    button.btn.btn-primary(type='submit') Submit



  if errors 
    ul
      for error in errors
        li!= error.msg

